class SchemaClass:
    old_schema_field: str
    random_field: str
    # random comment

    def old_schema_call(x: str):
        # in old schema call comment

        return x


def schema_function(x: str):
    length = len(x)
    return None
