enum Color {
  RED
  GREEN
  BLUE
}

input Filter {
  name: String
  shades: [Color!]
}

type Swatch {
  name: String!
  color: Color!
}

type Query {
  swatches(
    color: Color! = RED
    filter: Filter = { name: "any" }
    counts: [Int!] = [1, 2]
    flag: Boolean = true
  ): [Swatch!]!
}
